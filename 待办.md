
- [x] Qoder 不再依赖用户私有的 `~/.qoder/hooks/flash4-light.sh`。应用随包携带 Rust Hook Helper，安装/修复会把旧包装脚本配置迁移成原生 `command + args`，卸载也会清理旧条目。
- [x] Tauri 控制面板的“适配器”页已支持 Codex、Claude Code、Qoder 的 Hook 检测、一键安装、修复/重装、卸载和重新检测，并保留不属于本应用的配置。
- [x] 生产状态通道不使用 TCP 端口（Unix Socket / Windows Named Pipe）；Vite 开发端口从 `1420` 开始动态选择，端口占用时自动回退并同步给 Tauri。

- [x] Tauri 控制面板“全部会话”列表中的离线条目支持叉号移除；同一会话产生新事件后仍会按最新状态重新出现。
- [x] 浮窗会话列表保持叉号与 Agent 名称的竖直位置不变，水平方向为名称/状态内容预留空间，使内容右边界与叉号左边界保持 5px 间距。


- 你自己设计一个测试脚本， 针对多 agent、多会话、多状态，尤其注意多状态串行流转和并发，把主要的流程都跑一遍，不要臆测
- 中英文切换按钮由上下布局改为左右布局，且浮窗中的工作状态要适配中英文


- 帮我传到我的 github 仓库

- 生成 macOS 的 dmg 安装包

- - 写一个 skill，让 ai 能通过这个 skill 安装我的 app


